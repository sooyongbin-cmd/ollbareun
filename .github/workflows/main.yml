name: Deploy education-reminders

on:
  push:
    branches:
      - main
    paths:
      - ".github/workflows/main.yml"
      - "supabase/functions/education-reminders/**"
      - "supabase/migrations/**"
      - "supabase/config.toml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest

    env:
      SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
      SUPABASE_DB_PASSWORD: ${{ secrets.SUPABASE_DB_PASSWORD }}
      SUPABASE_PROJECT_ID: wexcijqchwwkxpkwajbf

    steps:
      - uses: actions/checkout@v4

      - uses: supabase/setup-cli@v1
        with:
          version: latest

      - run: supabase link --project-ref "$SUPABASE_PROJECT_ID" --password "$SUPABASE_DB_PASSWORD" --yes
      - run: supabase db push --linked --yes

      - run: supabase functions deploy education-reminders --project-ref "$SUPABASE_PROJECT_ID"
