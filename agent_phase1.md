# Phase 1: 단순 출퇴근 MVP

가장 먼저 개발할 최소 단위는 **근태 기록을 실제로 데이터로 남기는 기능**이다.

## Scope

- 초기 화면
  - 초기 화면에는 `관리자` 버튼과 `경비원` 버튼만 표시
  - `관리자` 버튼 클릭 시 `/manager`로 이동
  - `경비원` 버튼 클릭 시 `/guard`로 이동
- 관리자 화면
  - `/manager` 경로에서 제공
  - 로그인 없이 접근
  - 좌측 사이드바 Navigation 제공
  - 직원등록, 근무지등록, 근무지배정은 각각 독립 페이지로 제공
  - `/manager/employee/employees/new`: 직원등록
  - `/manager/employee/worksites/new`: 근무지등록
  - `/manager/employee/assignments/new`: 근무지배정
  - 직원 목록, 오늘 배정, 실시간 출근 현황 리스트 표시
  - 전체 인원 / 현재 출근 인원 요약 카드 표시
- 경비원 화면
  - `/guard` 경로에서 제공
  - 이름과 연락처 입력을 통한 단순 인증
  - 오늘의 근무지 표시
  - 현재 위치 확인
  - 근무지 반경 100m 이내에서만 `출근하기` 활성화
  - 출근 후 `퇴근하기` 가능
  - 오늘 근무 요약 표시
- 기본 데이터
  - 직원/경비원
  - 직원이름
  - 연락처
  - 근무지
  - 근무지별 GPS 좌표와 허용 반경
  - 직원별 근무지 배정
  - 출근/퇴근 기록

## Completion Criteria

- 관리자가 로그인 없이 관리자 화면에 접근할 수 있다.
- 초기 화면에서 `관리자` 버튼을 누르면 관리자 화면으로 이동한다.
- 초기 화면에서 `경비원` 버튼을 누르면 경비원 화면으로 이동한다.
- 관리자가 직원이름과 연락처로 직원을 등록할 수 있다.
- 관리자가 근무지를 등록할 수 있다.
- 관리자가 직원에게 근무지를 배정할 수 있다.
- 경비원이 이름과 연락처를 입력하면 직원 테이블과 비교해 단순 인증된다.
- 경비원이 모바일 화면에서 GPS 조건을 만족하면 출근할 수 있다.
- 퇴근 기록까지 저장된다.
- 관리자가 현재 출근 현황을 볼 수 있다.

## Test Plan

- 초기 화면에는 `관리자`, `경비원` 버튼만 표시
- `관리자` 버튼은 `/manager`로 이동
- `경비원` 버튼은 `/guard`로 이동
- 관리자 화면은 로그인 없이 접근 가능
- 관리자 좌측 메뉴에는 직원등록, 근무지등록, 근무지배정 링크가 있음
- 직원등록 페이지에서 직원이름과 연락처를 저장할 수 있음
- 근무지등록 페이지에서 근무지명, 위도, 경도, 허용 반경을 저장할 수 있음
- 근무지배정 페이지에서 직원에게 오늘 근무지를 배정할 수 있음
- 경비원 화면에서 등록된 이름/연락처 입력 시 인증 성공
- 등록되지 않은 이름/연락처 입력 시 인증 실패
- 이름은 같지만 연락처가 다르면 인증 실패
- 근무지 반경 안에서는 출근 버튼 활성화
- 근무지 반경 밖에서는 출근 버튼 비활성화
- 출근 전에는 퇴근 불가
- 출근 후 퇴근 가능
- 하루 출근/퇴근 기록이 관리자 현황에 반영
