# 저장·삭제 비동기 처리 공통 UX

## 목적

전체화면의 저장·삭제 작업은 네트워크와 서버 처리 시간에 따라 즉시 완료되지 않을 수 있다. 사용자가 버튼을 누른 뒤 작업이 진행 중인지 알 수 있도록 확인 모달을 처리 완료 시점까지 유지하고, 명확한 상태 문구와 로딩 효과를 제공한다.

## 공통 동작

1. 삭제 버튼을 누르면 삭제 확인 모달을 연다.
2. 저장 확인 모달을 사용하는 화면에서는 저장 버튼을 누르면 저장 확인 모달을 연다.
3. 확인 모달의 `예`를 누르면 즉시 모달을 닫지 않고 비동기 작업을 시작한다.
4. 작업 중에는 모달 안에 다음 상태를 표시한다.
   - 삭제: `삭제처리중입니다...`
   - 저장: `저장처리중입니다...`
   - 기타 동일한 비동기 확인 작업: 작업 목적을 설명하는 `...중입니다...` 문구
   - 회전형 로딩 아이콘과 `role="status"`, `aria-live="polite"`를 함께 제공한다.
5. 작업 중에는 확인·취소 버튼, 모달 닫기, 동일 작업을 다시 실행하는 입력을 비활성화한다.
6. 성공 또는 실패 응답을 받은 뒤에만 처리 중 상태를 해제한다. 성공 시 결과 안내 또는 다음 화면 이동을 수행하고 확인 모달을 닫는다. 실패 시 오류 메시지를 표시하고 재시도할 수 있는 상태로 되돌린다.
7. 중복 요청을 방지하기 위해 동일 작업의 처리 상태에서는 버튼과 제출 이벤트를 다시 실행하지 않는다.

## 구현 기준

- 공통 확인 모달은 `src/components/modals/confirm-modal.tsx`의 `ConfirmModal`을 사용한다.
- 확인 없이 즉시 시작되는 저장·업로드 작업은 `src/components/modals/processing-modal.tsx`를 사용하되, 같은 문구·스피너·입력 잠금 규칙을 적용한다.
- 비동기 상태는 화면 컴포넌트가 `saving`, `deleting`처럼 작업별로 소유하고, 모달에는 `loading`과 작업별 `loadingLabel`을 전달한다.
- `loading`이 `true`인 동안 `ConfirmModal`은 열린 상태를 유지하고 로딩 문구·아이콘만 표시한다. 부모 컴포넌트는 `await`가 끝난 뒤 상태를 해제하고 모달을 닫는다.
- 브라우저 기본 `window.confirm`과 `window.alert`는 사용하지 않는다. 기존 화면을 수정할 때도 공통 `ConfirmModal`·`AlertModal` 조합으로 교체한다.
- 로딩 문구는 색상만으로 표현하지 않고 텍스트와 아이콘을 함께 사용한다. 포커스가 모달 밖으로 빠지지 않으며 키보드 사용 시에도 같은 상태를 확인할 수 있어야 한다.

## 완료 조건

- 느린 네트워크 응답 중 모달이 사라지지 않고 처리 중 문구와 스피너가 보인다.
- 요청이 끝나면 모달이 닫히고 성공 안내·화면 이동 또는 오류 안내가 정상적으로 이어진다.
- 취소를 누른 경우에는 요청을 보내지 않고 현재 화면을 유지한다.
- 저장·삭제 버튼을 빠르게 여러 번 눌러도 서버 요청이 중복되지 않는다.
- 저장·삭제가 있는 관리자 화면에서 동일한 문구, 로딩 표현, 비활성화 규칙을 사용한다.
