# 경비원 메인 화면 디자인 분석 및 변경 계획

작성일: 2026-09-11

상태: 디자인 분석 및 구현 계획. 이 문서 작성 단계에서는 프로그램을 수정하지 않는다.

## 1. 목표와 기준

`/guard/main`의 기존 시각 디자인을 전면 교체하고, 아래 Figma의 `Frame 1`을 새 디자인 기준으로 사용한다. 기존 배치, 색상, 카드 구성, 메뉴 모양은 유지 조건이 아니다. 인증, 배정 확인, 출퇴근 상태, 위치 확인 등 서비스 동작은 새 화면에 연결한다.

- 변경 대상: https://ollbareun.vercel.app/guard/main
- 디자인 원본: https://www.figma.com/design/FZPKEdo37LS5msppwkethy/Untitled?node-id=4-4&m=dev
- Figma 파일: `FZPKEdo37LS5msppwkethy`, 기준 노드: `4:4`
- 기존 치수 시트: https://docs.google.com/spreadsheets/d/1VCV_0goRM18G6MUlX6LiwVT83ZkolyJGDBNoEphiLHc/edit

**직전 조회에서 확인한 원본 크기는 420 × 681px이다.** 이전 `guard` 시트에 기록한 360 × 681px 이후 원본 너비가 변경됐다. 치수표는 해당 시점의 측정 기록이다. 구현은 아래 사용자 확정 규칙을 우선하며, 특히 681px를 CSS 높이로 사용하지 않는다. 기존 시트는 이전 측정 기록으로 취급하고 이번 작업에서는 갱신하지 않는다.

확인 근거는 다음과 같다.

| 근거 | 확인한 내용 | 한계 |
| --- | --- | --- |
| Figma `4:4` 디자인 컨텍스트 및 이미지 | 배치, 색상, 글꼴, 그림자, 아이콘, 문구 | 정적인 한 상태의 시안이며 상호작용 명세가 아님 |
| Figma `4:4` 최신 메타데이터 | 420 × 681px 기준의 내부 노드 치수 및 좌표 | 좌표는 각 부모 노드 기준 |
| 운영 페이지 브라우저 | 현재 로그인 세션의 메인 화면, 미화원 역할의 메뉴, 하단 4개 메뉴 | 모든 역할·오류·출퇴근 상태를 실행한 것은 아님 |
| 로컬 저장소 | 화면 구성 파일, 데이터 타입, 기존 이동·권한 처리 | 배포본과 로컬 소스의 커밋 일치 여부는 확인하지 않음 |

이하에서 **원본 확인값**은 조회 당시 Figma에 실제 존재하는 값이다. **사용자 확정 규칙**은 후속 요청으로 확정된 구현 기준이며 원본 측정값과 기존 제안보다 우선한다. **구현 제안**은 실제 데이터와 연결 페이지 등 아직 확정하지 않은 동작의 계획이다.

## 2. 화면 구조

### 2.1 사용자 확정 구조

Frame 1은 **`body` 태그 바로 아래의 첫 번째 `div`**이다. 이 요소를 감싸는 별도 `div`를 추가하지 않는다. Frame 1의 직접적인 화면 구성은 header, contents, footer 세 부분이다. contents라는 명칭으로 문서 본문 영역을 구분하며, HTML의 `body` 태그와 혼동하지 않는다.

```text
body
└─ div#frame1 — 첫 번째 div, width: 100%, max-width: 420px, 가운데 정렬, 높이 Hug
   ├─ header#frame2 — 좌우 padding 20px
   │  ├─ #logo — 사용자가 제공할 SVG 파일
   │  └─ 사용자 이름(역할)
   ├─ main#frame20.contents — 좌우 padding 20px
   │  ├─ section — margin-top: 20px, 오늘 근무 정보 + 출근하기 버튼
   │  └─ section — margin-top: 20px, 네 개의 메뉴 버튼
   └─ footer#frame30 — Frame 1의 하단, 다섯 개의 내비게이션 항목
```

위 ID는 구현 구조를 설명하기 위한 명칭이다. **Frame 20은 두 섹션 전체를 포함하는 contents로 정의한다.** 이전 Figma 조회에서는 Frame 12가 근무 카드를, Frame 20이 네 개 메뉴만 감쌌지만, 그 중첩 구조를 그대로 복사하지 않는다. 아래 노드 표는 원본 식별을 위한 기록일 뿐 구현 DOM 구조를 결정하지 않는다.

Frame 21은 의미 없는 래퍼이므로 구현에서 제외한다. 원본에서 Frame 21 아래에 있는 Frame 30을 Frame 1의 footer로 직접 배치한다. Frame 21의 116px 높이나 56px 빈 공간을 다른 래퍼·패딩으로 재현하지 않는다.

Frame 1은 `width: 100%`, `max-width: 420px`, `margin-inline: auto`를 사용한다. 가용 뷰포트가 420px보다 크면 너비 420px를 유지하고 가운데 정렬한다. 높이는 콘텐츠에 따라 결정되는 Hug이며 CSS에 `height`를 선언하지 않는다. `min-height`, `max-height`, 뷰포트 높이 강제값으로 Hug를 우회하지 않는다.

header와 contents는 각각 좌우 padding 20px를 한 번만 적용한다. 첫 섹션 내부 카드의 padding 20px는 카드 자체의 안쪽 여백으로 별개다. 두 번째 섹션의 각 메뉴 버튼에는 바깥쪽 20px 패딩을 반복 적용하지 않는다. 첫 섹션과 두 번째 섹션은 **각각 `margin-top: 20px`**를 사용하며, Frame 1의 일괄 `gap`으로 이 여백을 중복시키지 않는다.

### 2.2 주요 영역의 위치와 치수 — 이전 원본 측정 기록

다음 좌표는 조회 당시 `Frame 1`의 왼쪽 위를 `(0, 0)`으로 환산한 값이며 단위는 px이다. **Height 및 Y는 원본 측정값이지 CSS 높이·절대 위치 지시가 아니다.** 구현 구조와 여백은 2.1절을 우선한다.

| 영역 | X | Y | Width | Height | 배치 규칙 |
| --- | ---: | ---: | ---: | ---: | --- |
| 전체 화면 | 0 | 0 | 420 | 681 | 구현은 max-width 420px, 높이 선언 없음, 배경 `#EFEFEF` |
| 헤더 | 0 | 0 | 420 | 60 | 흰색, 좌우 20 여백 |
| 로고 | 20 | 18 | 144 | 24 | 회사 심볼과 회사명 결합 |
| 사용자 표시 | 289 | 15.5 | 111 | 29 | 오른쪽 정렬, 모서리 5 |
| 오늘 근무 카드 | 20 | 80 | 380 | 211 | 내부 여백 20, 모서리 10 |
| 카드 제목 행 | 40 | 100 | 340 | 29 | 오늘 근무 / 근무지명 |
| 근무 일시 행 | 40 | 149 | 340 | 46 | 왼쪽 세로선, 날짜·시간·상태 |
| 출근하기 버튼 | 40 | 216 | 340 | 55 | 카드 내부 전체 너비 |
| 안전교육 메뉴 | 20 | 311 | 380 | 50 | 흰색 단일 메뉴 카드 |
| 순찰 메뉴 | 20 | 372 | 380 | 50 | 이전 메뉴와 간격 11 |
| 특이사항 보고 메뉴 | 20 | 433 | 380 | 50 | 이전 메뉴와 간격 11 |
| 근무 정보 메뉴 | 20 | 494 | 380 | 50 | 이전 메뉴와 간격 11 |
| 하단 영역 래퍼 Frame 21 | 0 | 564 | 420 | 116 | 구현에서 제외, 빈 공간도 복사하지 않음 |
| 하단 흰색 내비게이션 | 0 | 620 | 420 | 60 | 아이콘 + 문구, 5개 항목 |

원본 메뉴 묶음의 측정 높이는 `50 × 4 + 11 × 3 = 233px`이다. 단, 원본의 footer Y=620px, 전체 높이 681px, 메뉴 하단과 footer 사이의 76px 빈 공간은 구현 조건이 아니다. Frame 21을 제외하고 Hug 레이아웃을 적용하므로 footer 위치는 앞선 콘텐츠의 실제 크기에 따라 결정된다.

### 2.3 원본 내부 컨테이너 치수 목록 — 구현 DOM과 구분

아래는 최신 메타데이터의 `<frame>` 항목 30개이다. 같은 이름의 노드는 ID와 부모로 구분한다. `Group 1`은 이름이 Group인 컨테이너로 반환됐으며, 실제 편집기 노드 타입은 별도 확인 없이 FRAME이라고 단정하지 않는다.

| ID | 이름 | 부모 | Width | Height |
| --- | --- | --- | ---: | ---: |
| 4:4 | Frame 1 | Page 1 | 420 | 681 |
| 4:35 | Frame 2 | Frame 1 | 420 | 60 |
| 4:31 | logo | Frame 2 | 144 | 24 |
| 4:34 | name | Frame 2 | 111 | 29 |
| 8:30 | Frame 12 | Frame 1 | 420 | 211 |
| 8:6 | Frame 7 | Frame 12 | 380 | 211 |
| 8:7 | Frame 5 | Frame 7 | 340 | 95 |
| 8:16 | Frame 9 | Frame 5 | 340 | 29 |
| 8:10 | Frame 4 | Frame 5 | 340 | 46 |
| 8:27 | Frame 11 | Frame 4 | 329 | 46 |
| 8:25 | Frame 10 | Frame 11 | 329 | 23 |
| 8:21 | Group 1 | Frame 10 | 62 | 23 |
| 8:13 | Frame 6 | Frame 7 | 340 | 55 |
| 11:51 | Frame 20 | Frame 1 | 420 | 233 |
| 11:41 | Frame 16 | Frame 20 | 420 | 50 |
| 9:34 | Frame 14 | Frame 16 | 380 | 50 |
| 11:42 | Frame 17 | Frame 20 | 420 | 50 |
| 11:43 | Frame 14 | Frame 17 | 380 | 50 |
| 11:45 | Frame 18 | Frame 20 | 420 | 50 |
| 11:46 | Frame 14 | Frame 18 | 380 | 50 |
| 11:48 | Frame 19 | Frame 20 | 420 | 50 |
| 11:49 | Frame 14 | Frame 19 | 380 | 50 |
| 15:155 | Frame 21 | Frame 1 | 420 | 116 |
| 16:221 | Frame 30 | Frame 21 | 420 | 60 |
| 16:220 | Frame 29 | Frame 30 | 420 | 36 |
| 16:219 | Frame 28 | Frame 29 | 60 | 34 |
| 16:218 | Frame 27 | Frame 29 | 60 | 35 |
| 16:217 | Frame 26 | Frame 29 | 60 | 35 |
| 16:209 | Frame 22 | Frame 29 | 60 | 36 |
| 16:215 | Frame 24 | Frame 29 | 60 | 35 |

## 3. 시각 스타일 명세

### 3.1 색상과 모양 — 원본 확인값

| 용도 | 값 |
| --- | --- |
| 화면 배경 | `#EFEFEF` |
| 헤더, 카드, 하단 메뉴 배경 | `#FFFFFF` |
| 출근 버튼 배경, 근무지명 | `#005BAC` |
| 출근가능 배지 배경 | `#00B9EF` |
| 하단 홈 활성 문구 | `#0097E0` |
| 하단 비활성 문구 | `#898989` |
| 본문과 제목 | `#141414` |
| 사용자 표시 배경 / 글자 | `#EEF0F2` / `#1E1E1E` |
| 출근 버튼과 상태 배지 글자 | `#FFFFFF` |
| 근무 카드, 메뉴 카드, 출근 버튼 모서리 | `10px` |
| 사용자 표시, 상태 배지 모서리 | `5px` |
| 근무 카드와 메뉴 카드 그림자 | `0px 0px 4px 0px rgba(0, 0, 0, 0.25)` |

카드에 별도 테두리 색상은 디자인 컨텍스트상 지정되지 않는다. 캡처에서 보이는 얇은 외곽은 그림자와 함께 비교한다. 헤더와 하단 메뉴도 기존 화면의 테두리·반투명·블러를 기본값으로 가져오지 않는다.

근무 일시 왼쪽 세로선은 높이 44px, 출력 폭 약 3px인 벡터다. 청색 계열인 것은 이미지에서 확인했으나 정확한 stroke 색은 이번 컨텍스트에 노출되지 않았다. 구현 시 `8:11`의 원본 SVG를 확인하여 사용한다.

### 3.2 타이포그래피 — 원본 확인값

폰트는 Pretendard이며 기본 확대율 100%에서 아래 크기를 기준으로 한다.

| 요소 | 크기 | 두께 | 줄 높이 | 비고 |
| --- | ---: | ---: | --- | --- |
| 오늘 근무 | 24px | 700 | normal, 노드 높이 29px | 카드 왼쪽 제목 |
| 근무지명 | 16px | 500 | normal, 노드 높이 19px | 오른쪽 정렬 |
| 날짜 | 16px | 600 | 23px | 예: `10/28 (금)` |
| 근무시간 | 16px | 400 | 16px | 예: `10:00 PM - 6:00 AM` |
| 출근가능 | 13px | 400 | 13px | 62 × 23px 배지 |
| 출근하기 | 18px | 600 | 25px | 중앙 정렬 |
| 주요 메뉴 4개 | 17px | 500 | 23px | 중앙 정렬 |
| 사용자 이름·역할 | 13px | 400 | normal, 노드 높이 16px | 예: `마동석(경비원)` |
| 하단 메뉴 문구 | 12px | 500 | 12px | 홈 외 문구 자간 -0.36px |

`normal`로 제공된 줄 높이와 노드의 실제 높이는 구분한다. 노드 높이를 모든 경우의 CSS line-height로 단정하지 않고, Pretendard 로딩 후 실제 렌더링으로 맞춘다.

### 3.3 로고와 아이콘

헤더의 `#logo`는 심볼과 `주식회사 올바름` 글자를 포함하는 144 × 24px 자산이다. **로고 SVG는 사용자가 별도로 제공한다.** Figma에서 로고를 다운로드하는 작업은 진행하지 않는다. 전달받은 파일을 저장한 뒤 Frame 2의 `#logo`에서 사용하며, 분할 벡터를 손으로 다시 그리거나 기존 로고로 임의 대체하지 않는다.

저장 예정 경로는 `public/guard/logo.svg`, 향후 화면에서 사용할 경로는 `/guard/logo.svg`다. 전달받은 SVG의 `viewBox`와 비율을 보존하며 기준 표시 크기는 144 × 24px다. **아직 로고 파일을 전달받지 않았고 위 경로에도 이번 작업으로 파일을 생성하지 않았다.** 파일 제공 후 저장과 연결을 진행한다.

하단 아이콘은 다음의 채워진 형태다. 현재 사용 중인 lucide 외곽선 아이콘과 시각적으로 다르다.

| 메뉴 | Figma 인스턴스 | 노드 ID | Width × Height |
| --- | --- | --- | --- |
| 홈 | house-fill | 16:186 | 16 × 16 |
| 안전교육 | play-btn-fill | 15:149 | 16 × 17 |
| 순찰 | check-square-fill | 15:52 | 16 × 16 |
| 특이사항 | chat-dots-fill | 15:72 | 16 × 17 |
| 근무정보 | calendar2-week-fill | 15:67 | 16 × 16 |

구현 때 하단 아이콘은 원본 자산을 내보내 정적 파일로 보관한다. Figma 도구의 임시 asset URL은 약 7일 후 만료되므로 운영 코드에 그대로 저장하지 않는다. 로고는 사용자가 제공하는 SVG 파일을 사용한다. 이번 문서 작업에서는 자산을 추가하지 않았다.

하단 항목의 너비는 각 60px, 항목 간격은 7px이다. 총 너비는 328px이며 420px 화면에서 좌우 여백은 각각 46px이다. 따라서 원본을 정확히 재현할 때는 화면 전체를 단순 5등분하는 배치와 차이가 있다. 아이콘과 문구는 footer 내부를 기준으로 배치하며, Frame 1의 Hug 높이에 맞춰 함께 이동한다. 클릭 영역은 시각 요소보다 넓게 확보할 수 있다.

### 3.4 guard 전체에서 사용하는 기본 버튼 — 사용자 확정 규칙

**첫 번째 섹션의 `출근하기` 버튼을 guard 디자인 전체의 기본 버튼으로 정의한다.** `/guard/main`뿐 아니라 향후 `/guard` 이하의 여러 화면에서 공통 컴포넌트 또는 공통 스타일로 재사용한다. 페이지마다 색상·패딩·글꼴을 새로 정의하지 않는다.

| 속성 | 기본값 | 근거 |
| --- | --- | --- |
| 너비 | `width: 100%` | 부모 콘텐츠 영역 전체 |
| 위·아래 padding | 각각 `15px` | 원본 출근하기 버튼 |
| 왼쪽·오른쪽 padding | 각각 `0` | 사용자 확정값; 원본 컨텍스트의 좌우 20px보다 우선 |
| 텍스트 정렬 | 가운데 | `text-align: center` |
| 배경 | `#005BAC` | 원본 출근하기 버튼 |
| 글자색 | `#FFFFFF` | 원본 출근하기 버튼 |
| 글꼴 | Pretendard / Pretendard Variable | 기존 폰트 파일 활용 |
| 글자 크기 | `18px` | 기본 확대율 100% 기준 |
| 글자 굵기 | `600` / SemiBold | 원본 확인값 |
| 줄 높이 | `25px` | 원본 확인값 |
| 모서리 | `10px` | 원본 확인값 |
| 테두리 / 그림자 | 없음 | 기본 CTA 기준 |
| 높이 | 별도 선언 없음 | 한 줄은 25px + 상하 15px로 결과 높이 55px |

다음은 문서상의 CSS 명세이며 실제 프로그램에 적용한 코드는 아니다.

```css
.guard-button {
  box-sizing: border-box;
  display: block;
  width: 100%;
  padding: 15px 0;
  border: 0;
  border-radius: 10px;
  background-color: #005bac;
  color: #ffffff;
  font-family: "Pretendard Variable", "Pretendard", sans-serif;
  font-size: 18px;
  font-weight: 600;
  line-height: 25px;
  text-align: center;
}
```

높이는 고정하지 않으며, 줄바꿈·글자 확대 시 텍스트와 상하 패딩에 따라 자연스럽게 늘어난다. 기본 스타일에 고정 높이 유틸리티나 좌우 패딩을 섞지 않는다. 링크를 버튼 모양으로 사용할 때도 같은 스타일을 사용하고 동작에 맞는 HTML 의미를 유지한다. 포커스·비활성·처리 중 상태는 공통 컴포넌트에서 정의하며 클릭 중복 방지와 키보드 접근성을 유지한다.

두 번째 섹션의 네 개 흰색 버튼은 **공통 버튼의 메뉴 변형**으로 정의한다. 너비 100%, 좌우 padding 0, 가운데 정렬, 모서리 10px를 공유하며, 원본에 맞춰 흰 배경·`#141414` 글자·17px/500/23px 글꼴·카드 그림자로 변형한다. 원본의 결과 높이 50px는 상하 padding 각각 13.5px로 재현할 수 있으며, `height: 50px`를 강제하지 않는다. 네 버튼 사이 간격은 11px다. 이는 새로운 기본 버튼을 별도로 만드는 것이 아니라 같은 버튼 체계의 명시적인 변형이다.

## 4. 현재 구현에서 새 디자인으로 바뀌는 부분

저장소는 Next.js 16 / React 19 / TypeScript / Tailwind CSS 4 및 shadcn 기반이다. 새 라이브러리 도입 없이 현재 도구로 구현할 수 있다. 시각적 기준은 Figma를 우선한다.

| 현재 구조 | 새 디자인 | 처리 방향 |
| --- | --- | --- |
| 직군 아이콘·제목 + 이름·근무지 텍스트 헤더 | 회사 로고 + 이름(직군) 배지 | 헤더 DOM과 스타일 재구성 |
| 메인 상하 `5rem` 패딩, 최대 600px 내부 폭 | 헤더 아래 20px 간격, 모바일 카드 구성 | 기존 바깥 배치 제거 후 전용 레이아웃 구성 |
| 근무지·배정기간·NFC체크포인트 상세 카드 | 오늘 근무 + 근무지 + 날짜·시간·상태 | 홈에는 요약 카드, 상세는 근무 정보 접근 경로 검토 |
| 출퇴근과 QR/NFC 등 버튼 묶음 | 카드 안 파란 출근 CTA + 주요 메뉴 4개 | 업무 동작은 연결하고 표현을 새로 구성 |
| 안전교육 이수현황 별도 카드 | 안전교육 메뉴 한 줄 | 홈 상세 카드를 제거하고 교육 화면의 정보 접근 확인 |
| 푸시 등록 진행 또는 완료 표시 | 원본에 해당 패널 없음 | 구독 동작과 표시를 분리, 정상 상태 패널은 홈에서 숨기는 방향 |
| 홈 / 출퇴근 / 업무 / 내 정보, 하단 4개 | 홈 / 안전교육 / 순찰 / 특이사항 / 근무정보, 하단 5개 | 아이콘, 항목, 활성 상태 재구성 |

공통 `src/app/guard/main/layout.tsx`와 하단 내비게이션은 하위 페이지에서도 사용된다. 따라서 홈만 바꾸더라도 헤더·하단 변경 영향이 출퇴근, 교육, 순찰, 보고, 프로필에 미친다. **이번 계획의 상세 디자인 대상은 메인 본문이며, 하위 페이지 본문 전체의 재디자인은 별도 범위**다. 공통 요소를 바꿀 때 하위 화면의 상하 여백과 접근 경로는 함께 검증한다.

## 5. 실제 데이터와 기능 연결

### 5.1 원본 문구와 데이터

`마동석`, `롯데스카이힐`, `10/28 (금)`, `10:00 PM - 6:00 AM`은 디자인 샘플이다. 운영 화면에 하드코딩하지 않는다.

| 화면 정보 | 현재 확인한 데이터 | 구현 계획 |
| --- | --- | --- |
| 사용자 이름·직군 | `session.employee.name`, `role` | 이름(직군)으로 조합 |
| 근무지명 | `session.worksite.name` | 카드 우측 표시, 긴 이름 처리 |
| 기준 날짜 | `attendance.work_date`, 서울 기준 오늘 날짜 | 미종료 근무가 있으면 근무 기록 날짜 우선 |
| 배정기간 | `assignment.start_date`, `end_date` | 하루 근무시간과 구분, 상세 정보에 사용 |
| 예정 근무 시작·종료 시각 | 확인한 `AssignmentRow`, `WorksiteRow`, 세션 타입에 없음 | 데이터 공급 방식이 결정되기 전에는 `근무시간 미등록` 등의 명시적 대체 문구 제안 |
| 실제 출퇴근 시각 | `attendance.clock_in_at`, `clock_out_at` | 예정 시간으로 오인하여 대체하지 않음 |
| 휴무 여부 | `session.isDayOff` | 상태 표현에 반영 |
| 출퇴근 상태 | `getAttendanceStatus()` | 기존 열린 근무·당일 완료 판정 재사용 |

예정 시각을 반드시 표시하려면 근무 배정/스케줄 데이터와 관리자 입력 화면, API의 추가 설계가 필요할 수 있다. 이는 단순 디자인 변경을 넘어서는 기능 작업이므로 별도 범위로 명시한다. 이번 조사로 데이터베이스 전체에 해당 필드가 없다고 단정하지는 않는다.

날짜와 시간은 `Asia/Seoul`을 기준으로 한다. 원본의 AM/PM 표기를 재현하되 야간 근무의 익일 종료를 표현할 수 있어야 한다. 날짜를 배정기간의 시작일로 고정하면 안 된다.

### 5.2 상태별 표현 — 구현 제안

원본에는 `출근가능 / 출근하기` 상태만 있다. 다음은 기존 동작과 연결하기 위한 제안이며 새 Figma 변형이 확인된 것은 아니다.

| 조건 | 카드 상태 문구 제안 | CTA 및 처리 |
| --- | --- | --- |
| 배정 있음, 휴무 아님, 새 근무 시작 가능 | 출근가능 | 출근하기 → 기존 위치 확인 후 출퇴근 화면 |
| 미종료 출근 기록 있음 | 근무중 | 퇴근하기 → 기존 출퇴근 화면; 자정을 지나도 퇴근 가능 흐름 유지 |
| 당일 퇴근 완료 | 근무완료 | 완료 표시와 비활성 처리, 기존 당일 재출근 제한 확인 |
| 휴무이고 열린 근무 없음 | 휴무 | 휴무 안내와 출근 제한 정책에 맞는 처리 |
| 배정 없음 | 배정 없음 | 근무지 배정 안내, 잘못된 위치 요청이나 출근 동작 방지 |
| 데이터 조회 중 | 불러오는 중 | 카드 위치 유지, 출근가능으로 성급히 표시하지 않음 |
| 조회 실패 | 정보를 불러오지 못했습니다 | 재시도 안내, 기존 세션의 신뢰 상태 고려 |

열린 근무와 오늘의 휴무/배정 변경이 겹치면 퇴근 경로가 막히지 않도록 별도로 검증한다. 기존 위치 게이트는 배정 확인 후 위치 권한을 확인하지만, 메인 진입 전에 실제 근무지 반경 안인지 보장하지는 않는다. 따라서 원본의 `출근가능`을 GPS 검사 완료의 의미로 사용하지 않는다.

### 5.3 메뉴 목적지와 미정 사항

Figma는 정적 레이아웃이므로 아래 경로는 원본의 확정된 프로토타입 링크가 아니라 현재 서비스와 연결하기 위한 제안이다.

| 항목 | 경로 또는 연결 제안 | 확인할 사항 |
| --- | --- | --- |
| 홈 | `/guard/main` | 정확한 홈 경로에서 활성화 |
| 출근/퇴근 CTA | `/guard/main/attendance` | 현재 메인과 같이 진입 버튼으로 사용, 클릭 즉시 출근 기록을 생성하지 않음 |
| 안전교육 | `/guard/main/safety` | 본문과 하단 메뉴가 같은 목적지 사용 |
| 순찰 | `/guard/main/work`를 선택 화면으로 활용하는 안 | 현재 `/work`는 QR/NFC/보고가 있는 업무 화면이므로 그대로 순찰 전용 화면이라고 간주하지 않음 |
| NFC 실행 | `/guard/main/inspection-nfc` | 위치·배정 확인 유지 |
| QR 실행 | `/guard/main/inspection` | `USE_QR_CODE` 설정 반영 |
| 특이사항 보고 / 특이사항 | `/guard/main/special-remarks` | 하단 바로가기가 위치·배정 확인을 우회하지 않도록 처리 |
| 근무 정보 / 근무정보 | `/guard/main/profile`의 일정·월별 출퇴근 정보를 재사용하는 안 | 현재 `/work`는 근무정보가 아님. 프로필 통합으로 갈지 별도 화면을 만들지는 구현 전 선택 사항 |
| 사용자 배지 | `/guard/main/profile` 링크로 사용하는 안 | 기존 개인프로필·로그아웃·글자 확대·패스키 설정 접근 유지 |

순찰 메뉴를 하나로 줄여도 QR와 NFC 기능을 임의로 삭제하지 않는다. 현재 메인은 `USE_QR_CODE`에 따라 QR를 표시하지만 `/work`는 QR 링크를 직접 렌더링하므로, 선택 화면으로 활용할 경우 설정 처리를 일치시켜야 한다.

현재 서비스는 미화원·파견 역할도 같은 `/guard/main`을 사용한다. 경비원에서는 원본의 `순찰`, 미화원에서는 기존 `청소구역` 용어와 권한을 유지하는 방향을 제안한다. 파견 역할에서 순찰 항목을 숨길지 비활성화할지는 기존 역할 정책과 함께 정한다. 시안이 5개 메뉴라고 해서 모든 역할에 실행 권한을 추가하지 않는다.

## 6. 반응형과 CSS 적용 계획

### 6.1 반응형 구현 규칙

반응형 크기를 계산하는 `clamp()`, `min()`, `max()` 등의 함수를 사용하지 않는다. 크기 계산을 `calc()`로 대체하지도 않는다. **`max-width: 420px` 속성은 허용하며, 금지 대상인 `max()` 함수와 구분한다.** 너비 100%, 고정 최대 너비, padding, flex와 필요한 미디어 쿼리로 구성한다.

1. 기준 비교는 너비 **420px, 브라우저 확대 100%, 서비스 글자 확대 100%**로 한다. 681px는 과거 원본의 측정 높이이며 구현 높이나 footer 위치의 완료 기준이 아니다.
2. Frame 1은 `body` 바로 아래 첫 번째 `div`로 구성하고, `width: 100%`, `max-width: 420px`, 좌우 auto margin을 적용한다. 420px 초과 뷰포트에서도 Frame 1은 420px다.
3. Frame 1에는 `height`를 선언하지 않는다. `min-height`, `max-height`, `100vh`/`100dvh`, 강제 세로 늘림 등으로 콘텐츠 높이를 고정하거나 화면 높이에 맞추지 않는다.
4. header와 contents에 각각 `box-sizing: border-box`, 좌우 padding 20px를 적용한다. 첫 번째 섹션과 두 번째 섹션은 각각 위 margin 20px다.
5. 카드와 버튼 너비는 부모 안에서 100%를 사용한다. 예를 들어 Frame 1 너비 420px일 때 카드 380px, 카드 내부 버튼 340px가 padding의 결과로 만들어진다. 360px일 때는 각각 320px와 280px다. 이 수치를 CSS 함수로 계산하지 않는다.
6. footer는 Frame 1의 세 번째 부분으로 contents 뒤에 놓인다. **하단은 Frame 1의 콘텐츠 흐름 마지막 위치로 해석한다.** 기존 뷰포트 하단 고정 제안은 제거하고, 임의의 fixed/absolute 배치나 Y=620px 강제를 적용하지 않는다. Frame 21과 그 높이·빈 공간도 제외한다.
7. 하단 5개 항목은 60px 너비 + 7px 간격으로 중앙 정렬한다. 좁은 가용 너비는 flex 축소 또는 미디어 쿼리로 대응하며 계산형 크기 함수를 사용하지 않는다.
8. 카드와 버튼은 내용·padding·간격으로 높이가 결정된다. 근무 카드 211px, 기본 버튼 55px, 메뉴 버튼 50px는 기본 한 줄 상태의 참고 결과이며 고정 높이로 선언하지 않는다. 긴 내용과 글자 확대에서 높이가 자연스럽게 늘어난다.

### 6.2 구조 및 CSS 명세 예시

다음 예시는 문서의 구현 기준이며 프로그램 변경이 아니다. 실제 루트 레이아웃과 provider 구성을 확인해 동일한 DOM 구조를 만족시킨다.

```css
#frame1 {
  box-sizing: border-box;
  width: 100%;
  max-width: 420px;
  margin-inline: auto;
  background-color: #efefef;
}

#frame2,
#frame20 {
  box-sizing: border-box;
  padding-inline: 20px;
}

#frame20 > section {
  margin-top: 20px;
}

.guard-work-card {
  box-sizing: border-box;
  width: 100%;
  padding: 20px;
}

.guard-menu-section {
  display: flex;
  flex-direction: column;
  gap: 11px;
}

#frame30 {
  background-color: #ffffff;
}
```

Frame 1 높이 선언은 의도적으로 없다. 화면에 추가할 header, contents, footer 외의 불필요한 레이아웃 래퍼도 두지 않는다. footer 내부 내비게이션이나 카드 내부 정보 정렬에 필요한 요소는 각 영역 안에 둔다.

### 기존 스타일과 충돌하는 지점

- `src/app/globals.css`의 `.guard-font-scale :where([data-slot="button"], …)`는 버튼·배지 등의 폰트 크기를 `1rem × 확대율`로 설정한다. 기본 스타일 우선순위에 따라 Figma의 18px/17px/13px가 덮어써질 수 있으므로 실제 computed style로 확인한다.
- `.guard-font-scale`은 버튼 등에 최소 높이 `3rem × 확대율`도 적용한다. 사용자 배지를 공용 Button으로 구성하면 목표 29px와 충돌할 수 있다.
- 현재 `GuardZoomScope`가 `div`를 렌더링하므로 Frame 1 바깥에 이를 그대로 두고 안쪽에 또 Frame 1을 만들면 첫 번째 `div` 조건을 어긴다. 확대 관련 클래스와 상태를 Frame 1에 통합하는 등 실제 렌더링 DOM을 기준으로 구조를 정한다.
- `.guard-zoom-scope`에는 CSS `zoom`이 적용된다. 420px 기준 크기와 서비스 글자 확대를 구분해 검증한다. 기존 글자 확대 계산 방식은 확인 대상이며, 새 반응형 스타일의 크기 계산 함수 금지 규칙을 우회하는 수단으로 사용하지 않는다.
- 기존 `--primary`와 Tailwind primary는 `#0066CC`다. 새 CTA의 `#005BAC`, 배지의 `#00B9EF`, 활성 메뉴의 `#0097E0`는 서로 다른 값이다.
- 전역 primary 변경으로 관리자·홈페이지까지 바꾸지 말고, guard 전용 스타일/토큰 범위에서 새 원본 값을 정의한다. 기존 공용 Button은 동작·접근성 재사용 여부와 실제 외형 재현 여부를 따로 판단한다.
- Pretendard Variable 폰트 파일과 `--font-pretendard`는 이미 존재한다. 추가 다운로드보다 기존 폰트 로딩과 400/500/600/700 굵기 적용을 먼저 확인한다.

## 7. 파일별 예정 작업

아래 경로는 저장소 루트 기준이다. 모두 향후 구현 대상 또는 검토 후보이며, 이번 문서 작성으로 수정한 파일은 아니다.

| 파일 | 예정 작업 |
| --- | --- |
| `src/app/layout.tsx`, `src/app/guard/layout.tsx` | body의 첫 div가 Frame 1인지 검증, provider와 guard 전용 래퍼의 DOM 정리 검토 |
| `src/app/guard/main/page.tsx` | 기존 본문 배치 전면 교체, 근무 카드·4개 메뉴 조합, 역할과 설정 연결 |
| `src/app/guard/main/layout.tsx` | Frame 1 안의 header/contents/footer 3단 구성, 좌우 padding, 최대 너비 420px, Hug 높이 |
| `src/app/guard/main/guard-session-summary.tsx` | 사용자 이름(역할) 배지 표현 및 프로필 접근 제안 적용 |
| `src/app/guard/main/guard-header-title.tsx` | 직군 제목의 헤더 사용을 로고로 대체; 다른 참조 확인 후 처리 |
| `src/app/guard/main/guard-worksite-section.tsx` | 오늘 근무 카드로 재구성하거나 신규 카드로 분리, 상세 데이터의 이동처 검토 |
| 신규 guard 공통 버튼 컴포넌트 또는 스타일 | 3.4절의 출근하기 기본 디자인과 흰색 메뉴 변형을 `/guard` 하위 화면에서 재사용 |
| `src/app/guard/main/guard-location-gate-link.tsx` | 위치·배정 확인을 유지하면서 guard 공통 기본/메뉴 버튼 스타일 연결 |
| `src/app/guard/main/guard-bottom-navigation.tsx` | Frame 30 footer, 5개 항목, 원본 아이콘, 현재 경로 표시; Frame 21 및 기존 하단 고정 제거 |
| `src/app/guard/main/work/page.tsx` | 순찰 진입 화면으로 채택할 경우 QR 설정·역할 처리 및 명칭 조정 |
| `src/app/guard/main/profile/page.tsx` | 근무정보 목적지로 채택할 경우 정보 구성 및 기존 설정 접근 확인 |
| `src/app/guard/main/guard-safety-section.tsx` | 홈 상세 카드 제거 후 안전교육 페이지의 이수 정보 제공 여부 확인 |
| `src/app/guard/main/guard-push-register.tsx` | 푸시 등록 동작과 상태 표시를 분리, 정상 표시를 새 홈 구성에 맞게 정리 |
| `src/app/guard/main/attendance-status.ts` | 기존 판정 재사용; 디자인을 이유로 출퇴근 규칙 변경 금지 |
| `src/app/guard/guard-zoom-scope.tsx`, `src/app/globals.css` | 필요 범위에서 guard 글자 확대와 새 타입 크기의 양립 처리 |
| `public/guard/logo.svg` (제공 후 저장 예정) | 사용자가 제공하는 로고 SVG 저장 및 header의 `#logo`에 연결 |
| guard 아이콘 자산 디렉터리 | Figma 하단 아이콘 보관 |
| `src/app/guard/main/navigation.test.tsx`, `section-padding.test.ts` | 변경된 화면 구조·경로·패딩 기대값 조정, 기능 검증 유지 |
| `src/app/guard/main/guard-push-register.test.tsx` 및 관련 하위 페이지 테스트 | 푸시 동작과 공통 레이아웃 변경의 회귀 확인 |

공용 파일 삭제나 새 상세 페이지 추가는 사용처와 목적지를 확인한 후 별도로 결정한다. `GuardSessionGate`, 세션 저장, PWA 설치, 인앱 브라우저 안내 등의 동작을 기존 디자인 제거와 함께 무심코 없애지 않는다.

## 8. 실행 순서와 할 일

### 1단계 — 원본 및 기능 연결 확정

- [ ] 구현 시작 시 원본 스타일을 확인하되 사용자 확정 구조·Hug 높이·최대 너비 420px를 우선한다.
- [ ] 사용자가 제공하는 로고 SVG를 받은 후 `public/guard/logo.svg`에 저장한다. Figma 로고 다운로드는 하지 않는다.
- [ ] 하단 5개 아이콘의 원본을 확보한다.
- [ ] 예정 근무시간의 공급 방식 또는 미등록 표시 방식을 정한다.
- [ ] 순찰 목적지와 QR/NFC 선택 방식, 근무정보 목적지를 확정한다.
- [ ] 사용자 배지를 통한 프로필 접근, 미화원·파견 역할의 메뉴 표현을 정한다.
- [ ] 원본 하단 문구 `안전교욱`은 오탈자로 기록하고 서비스에서는 `안전교육`으로 교정하는 안을 적용한다.

### 2단계 — 공통 셸과 스타일

- [ ] guard 범위의 색·글꼴·모서리·그림자·간격을 정의한다.
- [ ] Frame 1을 body의 첫 번째 div로 구성하고 header/contents/footer만 주요 직접 자식으로 둔다.
- [ ] Frame 1의 width 100%, max-width 420px, 가운데 정렬을 적용하고 높이 관련 강제 선언을 제거한다.
- [ ] 반응형 스타일에 `clamp()`, `min()`, `max()` 등 크기 계산 함수를 사용하지 않는다.
- [ ] 회사 로고와 사용자 배지 헤더를 구현한다.
- [ ] header와 contents에 좌우 padding 20px를 각각 적용한다.
- [ ] Frame 21을 제외하고 Frame 30을 contents 뒤의 footer로 구성한다.
- [ ] 출근하기를 기준으로 guard 공통 버튼을 정의한다. width 100%, padding 15px 0, 중앙 정렬, 18px/600/25px, 파란 배경과 흰 글자를 적용한다.
- [ ] 하단 5개 메뉴와 활성 경로 처리를 구현한다. QR/NFC 하위 경로도 순찰 활성 상태로 묶는다.
- [ ] 기존 글자 확대·전역 Button 스타일과의 충돌을 해결한다.

### 3단계 — 메인 본문 교체

- [ ] 오늘 근무 카드의 제목, 근무지, 세로선, 날짜, 시간, 배지, CTA를 구현한다.
- [ ] contents 안에 근무 카드 섹션과 네 버튼 섹션을 두고 각각 margin-top 20px를 적용한다.
- [ ] 네 개의 흰색 메뉴 버튼을 공통 버튼의 메뉴 변형으로 구성한다.
- [ ] Figma 샘플 데이터를 실제 세션 데이터로 연결한다.
- [ ] 기존 상세 근무·교육 카드와 푸시 상태 패널을 새 홈의 구성에서 정리한다.
- [ ] 위치 게이트를 통과하는 CTA·메뉴 연결과 기존 출퇴근 화면 진입을 확인한다.

### 4단계 — 상태와 회귀 검증

- [ ] 출근 전, 근무중, 퇴근 완료, 휴무, 배정 없음, 조회 중·오류 화면을 확인한다.
- [ ] 전날 출근 후 자정 경과, 휴무/배정 변경과 미종료 근무가 겹친 경우를 확인한다.
- [ ] 긴 이름·근무지명, 서비스 글자 확대, 브라우저 확대에서 잘림·겹침을 확인한다.
- [ ] 하단 모든 메뉴와 프로필·로그아웃·교육·순찰 경로가 접근 가능한지 확인한다.
- [ ] 본문 메뉴와 하단 메뉴에 동일한 역할·위치·배정 정책을 적용한다.
- [ ] 새 화면에 맞춰 관련 테스트를 조정하고 `npm run test:guard`와 변경 파일 lint를 수행한다.
- [ ] 필요 시 빌드로 타입·리소스를 검증한다. 현재 `npm run build`는 문서 빌드도 실행하므로 생성 파일 변경을 별도로 확인한다.

## 9. 완료 기준

| 검증 | 완료 조건 |
| --- | --- |
| 기준 이미지 비교 | 너비 420px, 확대율 100%에서 사용자 확정 구조·padding·margin 및 원본 스타일 재현; 전체 681px 높이와 footer Y 좌표는 강제하지 않음 |
| DOM 구조 | body 바로 아래 첫 div가 Frame 1이며 header(Frame 2) / contents(Frame 20, 두 섹션) / footer(Frame 30) 구성, Frame 21 없음 |
| CSS 규칙 | Frame 1 높이 선언 및 뷰포트 높이 강제 없음, `clamp()`·`min()`·`max()` 등 반응형 크기 계산 함수 없음 |
| 공통 버튼 | guard 전체에서 재사용 가능, 기본 padding 15px 0 / width 100% / 가운데 정렬 / 파란 배경 / Pretendard 18px·600·25px |
| 시각 계층 | 회색 배경, 60px 로고 헤더, 오늘 근무 카드, 파란 CTA, 4개 흰색 메뉴, 하단 5개 항목 |
| 폰트·자산 | Pretendard 굵기·크기 적용, 사용자 제공 로고 SVG와 원본 하단 아이콘 사용, 깨진 임시 자산 URL 없음 |
| 화면 폭 | 360 / 390 / 420px에서 가로 스크롤 없음; 420px 초과 뷰포트에서도 Frame 1 너비 420px 및 가운데 정렬 |
| 화면 높이 | 콘텐츠에 따라 Frame 1이 늘어나며 footer는 contents 뒤에 위치, Frame 21의 빈 공간 없음, 긴 글자와 확대 시 잘림 없음 |
| 동적 정보 | 샘플 이름·날짜·시각 하드코딩 없음, 예정 시간 부재를 사실대로 표시 |
| 출퇴근 | 상태별 CTA·기존 위치 확인·중복 방지·야간 퇴근 경로 정상 |
| 접근성 | 키보드 포커스, 실제 버튼/링크 의미, 현재 메뉴 표시, 확대 시 가독성 확보 |
| 회귀 범위 | 관리자·공개 홈페이지 디자인 영향 없음, guard 하위 화면의 이동·권한·레이아웃 유지 |

원본의 작은 글자와 밝은 배지색은 대비 검증 대상이다. 접근성 대응으로 색이나 글자를 바꿔야 한다면 원본 확인값과 조정안을 구분해 기록한다. 정확한 일치 검증은 기본 상태에 적용하고, 오류·긴 텍스트·확대 상태에서는 정보가 읽히는 것을 우선한다.

## 10. 이번 작업 결과

기존 Figma 분석을 바탕으로 사용자 확정 규칙을 반영했다. 반응형 크기 계산 함수 금지, Frame 1의 최대 너비 420px·가운데 정렬·Hug 높이, header/contents/footer 구조, 두 섹션의 위 margin 20px, 공통 버튼 규칙과 사용자 제공 로고 사용을 문서에 명시했다. 프로그램 코드, API, 데이터베이스, Figma, 기존 Google Sheet 및 배포 상태는 수정하지 않았다. 로고는 사용자 제공 대기 상태이며, 앱 실행 테스트와 디자인 변경 검증은 아직 구현 전이므로 수행하지 않았다.
